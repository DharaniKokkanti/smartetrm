# Copy this file to .env (same folder) and fill in real passwords.
# Read by docker-compose.yml — never commit the real .env (already gitignored).
#
# ETRM_APP_DB_PASSWORD here MUST match DB_PASSWORD in etrm-backend/.env —
# this is the password db-init sets for the etrm_app login the backend
# connects as.

MSSQL_SA_PASSWORD=ChangeThisStr0ngP@ss!
ETRM_APP_DB_PASSWORD=ChangeThisAppP@ss!
